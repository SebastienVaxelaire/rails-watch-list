<div class="container mt-5">
<h1>Create a new movie list</h1>
<%= simple_form_for(@list) do |f| %>
<%= f.input :name %>
<%= f.input :photo, as: :file %>
<%= f.submit "Create the list", class: "btn btn-primary" %>
<% end %>
</div>
