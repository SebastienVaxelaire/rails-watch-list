<div class="container mt-5">
  <h1>Create a new bookmark</h1>
  <%= simple_form_for([@list, @bookmark]) do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie %>
    <%= f.association :list %>
    <%= f.submit "Create the bookmark", class: "btn btn-primary" %>
  <% end %>
</div>
